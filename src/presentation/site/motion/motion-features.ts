import { domAnimation } from "motion/react";

// Loaded on demand by MotionProvider so the animation engine stays out of the
// initial page JavaScript; content is visible before it arrives.
export default domAnimation;
